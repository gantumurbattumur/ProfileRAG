# Core FastAPI
fastapi==0.128.0
uvicorn==0.40.0
python-dotenv==1.2.1
pydantic==2.12.5
pydantic-settings==2.7.1
python-multipart==0.0.12

# OpenAI (lightweight, API-only)
openai==2.15.0

# FAISS for vector search (lightweight, CPU-only)
faiss-cpu==1.8.0.post1
numpy<2.0,>=1.24.0

# Security & Rate Limiting
slowapi==0.1.9
email-validator==2.3.0

# Email (Resend)
resend==2.19.0

# Testing
pytest==8.3.5
pytest-asyncio==0.24.0

# PDF parsing (for ingestion script only - optional at runtime)
pypdf==5.1.0
